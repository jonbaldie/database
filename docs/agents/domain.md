# Domain Docs

This is a single-context repository.

Before working, read root `CONTEXT.md` and relevant ADRs under `docs/adr/`. Use the glossary's canonical vocabulary and surface conflicts with recorded ADRs rather than silently overriding them. Missing domain files are created lazily by domain-modeling work.
